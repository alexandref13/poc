name: network_types_example
description: Demonstrates how to use the network_types plugin.
publish_to: 'none'

environment:
  sdk: ">=2.17.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  network_types:
    path: ../
  cupertino_icons: ^1.0.2
  permission_handler:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true